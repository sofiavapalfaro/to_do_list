<h1>Edit List</h1>

<%= form_with model: @list, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :done %>
  <%= f.check_box :done %>

  <%= f.submit "Update" %>
<% end %>
